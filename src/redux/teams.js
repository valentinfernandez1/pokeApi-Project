import * as ActionTypes from './ActionTypes';

export const Teams = (state = {
    isLoading: true,
    errMess: null,
    teams: []
    }, action) => {
    switch(action.type){
        default:
            return state;
    }
}
